services:
  postgres:
    image: postgres:16
    container_name: science_kit_bot
    environment:
      POSTGRES_DB: sciencekit_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5437:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: